{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run as processing",
            "type": "shell",
            "command": "processing-java.exe",
            "args": [
                "--sketch=${workspaceRoot}",
                "--run"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}